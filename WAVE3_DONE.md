# Wave 3 — сделано

## Что улучшено

### 1) История активности по дням
- В `engine10.js` добавлен `trainer_activity_<GRADE_NUM>`.
- История пишет последние 90 дней и обновляется вместе с `trainer_daily_*`.
- Сброс прогресса теперь очищает и историю активности.

### 2) Визуальный feedback при ответе
- В `engine10.css` добавлены анимации `flash-ok` и `flash-no`.
- В `engine10.js` добавлен `flashFeedback()` и вызов после ответа.
- Теперь реакция на ответ заметна даже на устройствах без вибрации.

### 3) Родительская панель стала полезнее
- В `dashboard.html` добавлен блок **«Активность за 14 дней»**.
- График агрегирует все задачи по всем классам.
- Добавлены кнопки:
  - `📄 Скачать сводку` — выгрузка `.txt`
  - `🖨 Печать / PDF` — печатная версия панели
- В экспорт попадают общие метрики, классы и слабые темы.

### 4) Аудит структуры контента
- Добавлен `curriculum_audit.js`.
- Сгенерирован `CURRICULUM_AUDIT.md`.
- Проверено:
  - все классы `1–11`
  - количество предметов и тем
  - наличие шпаргалок
  - размеры банков диагностики
  - повторяющиеся `subject/topic` между классами

## Проверки
- `python3 validate_release.py` → OK
- runtime smoke checks → OK
- curriculum audit → OK

## Важная оговорка
График активности начнёт заполняться полностью после новых сессий. Для уже старых установок он частично подхватит текущий `trainer_daily_*`, а дальше будет накапливаться автоматически.
