# Wave 16 — theory debt для 5–7 классов

## Что сделано

- Добавлен новый слой `wave16_theory.js`.
- Слой подключён к `grade5_v2.html`, `grade6_v2.html`, `grade7_v2.html`.
- Углублены шпаргалки для **48** целевых тем в `5–7` классах.
- Для каждой rich-шпаргалки теперь есть структура: заголовок, краткое объяснение, опорный блок `fm`, список правил и блок `ex`.
- Закрыт хвост из плана по `const→var` в `grade1`, `grade2`, `grade8`, `grade9`, `grade11`.
- Добавлен `theory_debt_audit.js` и новые guard'ы в `validate_release.py`.
- `sw.js` обновлён до `trainer-v14`, новый слой `wave16_theory.js` добавлен в precache.

## Проверяемые результаты

- Theory debt audit: **48 / 48** целевых тем прошли rich-theory guard.
- `grade5`: 20 тем, со шпаргалкой 20.
- `grade6`: 20 тем, со шпаргалкой 20.
- `grade7`: 22 тем, со шпаргалкой 22.
- English vertical остаётся закрытым: **11** классов на целевом уровне, всего тем: **54 / 54**.
- Coverage regression нет: тем ниже `10` уникальных формулировок — **0**.

## Комментарий

В базовой сборке шпаргалки в `5–7` уже существовали, но были короткими и неоднородными по качеству. В этой волне фокус был не на самом факте наличия `th`, а на повышении их учебной ценности и введении отдельного аудита на rich-theory качество.
